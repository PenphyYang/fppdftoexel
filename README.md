# 发票分析工具

这是一个带图形界面的 Python 工具，用来批量提取发票 PDF 内容，并导出为 Excel。

## 功能

- 支持一次选择多个 PDF 发票
- 自动提取发票基础信息
- 自动提取明细行
- 支持将明细中的 `规格型号` 单独拆分为一列
- 导出 `发票汇总` 和 `发票明细` 两个工作表
- 支持打包成单个 EXE

## 运行

```bat
E:\pyenv\pyenv-win\versions\3.8.10\python.exe invoice_pdf_to_excel_gui.py
```

## 命令行模式

```bat
E:\pyenv\pyenv-win\versions\3.8.10\python.exe invoice_pdf_to_excel_gui.py --input a.pdf b.pdf --output 结果.xlsx
```

## 打包 EXE

先确保已经安装依赖：

```bat
E:\pyenv\pyenv-win\versions\3.8.10\python.exe -m pip install -r requirements.txt
```

然后执行：

```bat
build_exe.bat
```

打包完成后的文件位置：

```text
dist\invoice_analysis_tool.exe
```

## 导出结果说明

Excel 中会生成两个工作表：

- `发票汇总`：每个 PDF 一行，包含发票号、日期、购销双方、金额合计等
- `发票明细`：提取到的明细项目，支持单独输出 `规格型号`

## 版本记录

### V1.0.1

封板日期：2026-04-02

对比 V1.0.0，本次版本重点放在“明细识别更通用、更稳定”。

新增：

- 新增跨页续行处理，支持上一页最后一条明细延续到下一页页首时自动拼接
- 新增按 PDF 表头列坐标识别 `项目名称` 与 `规格型号` 的通用分栏方式
- 新增对不同电子发票版式的兼容测试与修正

更新：

- 更新明细解析逻辑，尽量减少对固定关键词和特殊样本规则的依赖
- 更新 `规格型号` 提取策略，优先按版面列位置分栏，再结合续行进行拼接
- 更新 EXE 打包内容，同步包含最新跨页修复与更通用的识别逻辑

修复：

- 修复部分发票中“只是换行、不是规格型号”的内容被错误拆到 `spec_model` 的问题
- 修复某些五金发票中项目名称被错误截断、错误拆分的问题
- 修复部分多页发票中，明细内容跨页后页首续行丢失的问题
- 修复某些新版电子发票中明细整页被误跳过、导致识别为空的问题
- 修复 README 中文乱码问题

### V1.0.0

新增：

- 新增桌面 GUI，支持批量选择多个 PDF 文件
- 新增发票 PDF 提取与 Excel 导出能力
- 新增 `发票汇总`、`发票明细` 两个工作表输出
- 新增自定义软件名称、窗口标题与界面文案
- 新增软件 Logo 与 EXE 图标资源
- 新增 EXE 打包脚本与基础使用说明

更新：

- 更新导出结构，移除 `原始文本` 工作表
- 更新默认交互，输出路径改为空，开始处理时再选择保存位置
- 更新软件名为“发票分析工具”
- 更新窗口标题为“发票分析工具（ 只是一个路过的网友：Perfect）”

修复：

- 修复样本电子发票基础字段提取问题
- 修复明细金额、税额合计与发票汇总对账问题
- 修复打包时中文 EXE 名称导致的编码问题，统一为稳定英文文件名输出
- 修复 GUI、命令行、打包三种使用方式之间的兼容性问题

## 注意

- 当前解析规则针对常见电子发票版式做了适配
- 当前不支持扫描件图片型 PDF，只支持可提取文字的 PDF
- 少数复杂排版下，明细描述仍建议抽样人工校对
